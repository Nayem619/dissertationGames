PORT=8080

# Mongo session + Mongoose — use the same Atlas (or mongodb://…) URI for both.
CONNECTION_URI=

SESSION_SECRET=

# Comma-separated allowed browser origins when UI and socket host differ or you use Expo web.
# CORS_ORIGINS=https://dissertationgames-ludo.onrender.com,http://localhost:3000
