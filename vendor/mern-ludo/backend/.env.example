PORT=8080

# MongoDB (required)
MONGO_URI=

# Comma-separated dev origins — add http://YOUR_LAN_IP:3000 so a phone Expo client can reach the CRA dev server
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://10.0.2.2:3000,http://192.168.1.42:3000
